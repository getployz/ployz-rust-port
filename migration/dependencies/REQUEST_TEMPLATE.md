# Dependency request: `<capability>`

| Field | Value |
| --- | --- |
| Status | `research-required` |
| Capability | `<domain capability without a preferred crate>` |
| Waiting packets | `<packet paths>` |

## Required behavior

List the exact package behavior-contract rows this dependency must enable,
including observable limitations. Link each requirement to oracle evidence.

## Constraints

Record required platforms, targets, Rust version, licenses, security posture,
sync or async model, cancellation, performance, safety, and external runtime
requirements. Mark hard gates explicitly.

## Research assignment

The fresh researcher must compare all credible Rust ecosystem choices using
primary sources. It must prefer the popular, idiomatic solution among candidates
that pass every hard gate. A preferred crate named by a requester is only a
candidate, not a decision.

## Completion

Link the resulting decision record and required adversarial reviewer result.

